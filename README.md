download the zip file of the repository and extract it
then download https://github.com/7Har/Restaurant_management_system/files/6250009/get-pip.zip this zip file and extract it 

open the command prompt with get-pip directory and enter the following commands :-
python get-pip.py
py -m pip install django
pip install pycryptodome
pip install django-admin-rangefilter
python -m pip install Pillow    

open the command prompt with Restaurant_management_system directory and enter the following commands :-
python manage.py makemigrations
python manage.py migrate
python manage.py runserver

For Employee Login :- username:"admin" password:"123456789"
For Admin Login :- username:"dkp" password:"Dkp@4321"
